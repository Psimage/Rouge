allprojects {
    version = "1.0-SNAPSHOT"

    ext.appName = "Rouge"
    ext.gdxVersion = "1.1.0" // Use "1.1.1-SNAPSHOT" for nightly build

    repositories {
        mavenCentral()
        maven { url "https://oss.sonatype.org/content/repositories/snapshots/" }
        mavenLocal();
    }
}